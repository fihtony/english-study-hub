# Python
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.pyd
*.pyc

# Virtual environments
venv/
.venv/
ENV/
env/
env.bak/
venv.bak/

# Distribution / packaging
build/
dist/
*.egg
*.egg-info/
pip-wheel-metadata/
wheelhouse/

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
coverage.xml

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos.
*.manifest
*.spec

# Installer artifacts
instance/
.webassets-cache

# Pyre / mypy / pylance caches
.mypy_cache/
.pyre/
.pyright/

# IDEs and editors
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# macOS
.DS_Store

# Node / frontend
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock
.pnpm-debug.log
.next/
out/

# Caches
.cache/
*.sqlite3
*.db
*.db-journal

# Logs
logs/
*.log

# Secrets/env
.env
.env.*.local
.env.local
.env.test
secrets.json

# Build/test artifacts
*.log
*.tmp
*.bak

# Eggs and wheels
*.egg-info/
.eggs/

# Work evidence folder: ignore all runtime artifacts but keep the README and intentional artifacts
# The .work/ folder stores CI/test evidence and screenshots for PRs. By default ignore everything
# but explicitly allow README.md and other reviewed evidence files that MUST be committed.
.work/*
!.work/README.md
# If you intend to commit specific evidence files, add explicit negations here. Examples:
!.work/pytest-report.txt
!.work/screenshots/
!.work/screenshots/*.png
!.work/artifacts/

# OS generated files
Thumbs.db
desktop.ini

# Temporary files
*.swp
*.swo
*~
.idea/

# Editor backups
*.orig
*.rej

# End of file - keep this file at repo root to avoid committing local artifacts.
# If you need to keep additional files inside .work/, add explicit negation entries above.