# Python bytecode and caches
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
coverage.xml

# Flask instance folder (sensitive config)
instance/

# Environment variables file
.env
.env.*.local

# Editor and IDE directories
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# MacOS
.DS_Store

# Node
node_modules/

# Static assets should NOT be ignored; ensure build/static assets remain tracked.
# (No rule for /static/ is present on purpose.)

# Mypy/cache
.mypy_cache/
.dmypy.json

# Pyre
.pyre/

# JetBrains
*.iml

# Logs and databases
*.log
*.sqlite3
*.db

# Secrets (local)
secrets/
secrets-*.json

# Misc
thumbs.db
*.orig
*.bak
*.swp

# Keep Git safe; do not ignore Next.js build output (.next) here by intention.
# If Next.js is added to this repo later, update gitignore accordingly.