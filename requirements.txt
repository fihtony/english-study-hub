# Requirements for Python 3.12 — runtime and test dependencies
Flask>=2.2,<3
pytest>=7.0,<9
pytest-cov>=4.0,<5

# Optional packaging/build tools
wheel>=0.40,<1
setuptools>=68.0,<80

# Recommendation: upgrade pip before installing to use latest resolver and build backends:
# python3.12 -m pip install --upgrade pip